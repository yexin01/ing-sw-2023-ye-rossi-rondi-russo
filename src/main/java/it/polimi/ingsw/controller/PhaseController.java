package it.polimi.ingsw.controller;
/*
public class PhaseController<T extends Enum<T>> {
    private T currentPhase;

    public PhaseController(T initialPhase) {
        currentPhase = initialPhase;
    }

    public void changePhase() {
        T[] phases = currentPhase.getDeclaringClass().getEnumConstants();
        int nextIndex = (currentPhase.ordinal() + 1) % phases.length;
        currentPhase = phases[nextIndex];
    }

    public T getCurrentPhase() {
        return currentPhase;
    }

    public void setCurrentPhase(T currentPhase) {
        this.currentPhase = currentPhase;
    }
}

 */

